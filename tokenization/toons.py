

def intro_to_toons():
  process = """ JSON file format while human readable since they are verbose, have quotes (",") and special characters which provide visual clarity & human readable formating 
              but at the expence of more memory to represent same information. JSON is the request and responce format of many RESTful services but is not the best format to parse documents and feed it to LLMs, due to poor compression ratio.
              Hence, an alternative notation, Token-oriented Object Notion (TOON) has be introduced which provides better results rather traditional JSON structures.

            TOON provides better compression ratio and as it a format optimized for information concisely to LLMs hence requiring fewer tokens than it's JSON counterpart.
            """

  value_add = """ Toons is valuable due to the fact that LLM model usage is depending on $$$ per 1M input tokens or so, 
                  hence feeding same or more information for lower total input tokens using TOONs is a obvious choice """

